import React from 'react';
import { Chat as ChatWrapper } from "./ChatStyles";
import Header from '../../components/Header/Header';

const Chat = () => {
    return(
        <section>Chat</section>
    );
};

export default Chat;